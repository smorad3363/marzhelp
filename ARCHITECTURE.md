# معماری بازطراحی‌شده MarzHelp

## هدف

این نسخه برای اجرای `MarzHelp` روی همان میزبان `Marzban` و با دیتابیس
`MySQL` طراحی شده است. سرویس وب با کاربر سیستم محدود اجرا می‌شود و هیچ
مجوز مستقیم `sudo` دریافت نمی‌کند.

## مرزهای دسترسی

- کد برنامه متعلق به `root:www-data` و فقط خواندنی برای وب‌سرور است.
- داده‌های زمان اجرا در `/var/lib/marzhelp` نگهداری می‌شوند.
- نسخه‌های پشتیبان در `/var/backups/marzhelp` و فقط با دسترسی `root`
  نگهداری می‌شوند.
- کاربر `marzhelp_app` فقط دسترسی‌های روزمره
  `SELECT`, `INSERT`, `UPDATE`, `DELETE` دارد.
- کاربر `marzhelp_migrate` فقط برای مهاجرت و پشتیبان‌گیری استفاده می‌شود.
- اجرای به‌روزرسانی مرزبان، تغییر فایل‌های سیستم و دریافت اسکریپت خارجی
  از داخل ربات به‌صورت پیش‌فرض غیرفعال است.

## وبهوک تلگرام

در زمان نصب یا ارتقا یک مقدار تصادفی برای `webhookSecret` ساخته می‌شود.
تلگرام آن را در سرآیند زیر ارسال می‌کند:

`X-Telegram-Bot-Api-Secret-Token`

درخواست بدون این مقدار پیش از بارگذاری ربات و اتصال دیتابیس رد می‌شود.

## حسابداری ترافیک

مصرف جاری کاربران از جدول‌های استاندارد مرزبان خوانده می‌شود. هنگام حذف
کاربر، یک تریگر ثابت مجموع مصرف جاری و مصرف ریست‌شده را در جدول زیر ثبت
می‌کند:

`marzhelp_deleted_users`

هر کاربر فقط یک رکورد دارد و جدول بر اساس ادمین و زمان حذف ایندکس شده است.

حالت `used_traffic` شامل موارد زیر است:

1. مصرف جاری کاربران موجود
2. مصرف ریست‌شده کاربران موجود
3. مصرف نهایی کاربران حذف‌شده

حالت `created_traffic` شامل حجم فعلی اختصاص‌یافته کاربران محدود و حجم
ثبت‌شده کاربران حذف‌شده است. برای کاربران نامحدود، مصرف واقعی مبنا قرار
می‌گیرد.

## اعمال محدودیت‌ها

وضعیت محدودیت هر ادمین در جدول زیر نگهداری می‌شود:

`marzhelp_admin_enforcement`

دو تریگر ثابت فقط هنگام ساخت کاربر یا تغییر ادمین/حجم کاربر اجرا می‌شوند:

- `marzhelp_enforce_user_insert`
- `marzhelp_enforce_user_update`
- `marzhelp_enforce_user_delete`

این طراحی جایگزین حذف و ساخت دقیقه‌ای تریگرها شده است و محدودیت یک ادمین
را از ادمین دیگر مستقل نگه می‌دارد. سقف کاربر بر اساس تمام کاربران ادمین
محاسبه می‌شود، نه فقط کاربران فعال.

محدودیت‌های ساخت، حذف، ریست مصرف، لغو اشتراک و ساخت کاربر نامحدود نیز در
همین جدول سیاست نگهداری می‌شوند. تغییر آن‌ها به ساخت یا حذف تریگر توسط
وب‌سرور نیاز ندارد.

همگام‌سازی محدودیت ورودی‌ها با یک رویداد ثابت انجام می‌شود. فاصله اجرا در
جدول زیر ذخیره می‌شود و تغییر آن فقط یک عملیات داده‌ای است:

`marzhelp_runtime_settings`

## ارتقا و بازگشت

پیش از ارتقا، کد، تنظیمات و هر دو دیتابیس پشتیبان‌گیری می‌شوند. مهاجرت
جداول تکرارپذیر است و داده قدیمی `user_deletions` فقط برای پرکردن اولیه
دفتر جدید استفاده می‌شود.

برای بازگشت کامل باید هم کد قبلی و هم نسخه پشتیبان هر دو دیتابیس از مسیر
زیر بازیابی شوند:

`/var/backups/marzhelp/<timestamp>`

بازگشت فقط با تغییر تگ کد کافی نیست، زیرا نسخه جدید جداول و تریگرهای
حسابداری اضافه می‌کند.

اسکریپت `bootstrap.sh` نام تگ یا شاخه را دریافت می‌کند و همان مرجع را به
نصب‌کننده یا ارتقادهنده منتقل می‌کند. اگر ورودی داده نشود، شاخه
`main` انتخاب می‌شود.

## نکات ظرفیت

- اجرای زمان‌بندی‌شده دارای قفل است و دو اجرای هم‌زمان انجام نمی‌شود.
- تاریخچه `admin_usage` پس از ۴۰۰ روز پاک می‌شود.
- محاسبات حذف کاربران از ایندکس ترکیبی ادمین و زمان استفاده می‌کنند.
- پیش از استفاده در مقیاس بالا باید روی دیتابیس واقعی
  `EXPLAIN ANALYZE` و آزمون فشار انجام شود.
